# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies (root + every workspace)
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# turborepo
.turbo/

# testing
coverage/

# next.js (root + every workspace)
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/docs
/assets
# Raw store-template library (curated copies live in public/themes)
newstores-themes/

# Themes are served from private Supabase Storage (see scripts/upload-themes.mjs), not git
public/themes/
