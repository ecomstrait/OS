// Type-only barrel — safe to import from anywhere. For runtime helpers import
// the explicit entry: "@ecomstrait/auth/client" | "/server" | "/session" | "/middleware".
export type { Profile, UserRole } from "@ecomstrait/db/types";
