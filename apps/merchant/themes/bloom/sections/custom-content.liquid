{% comment %}
  Replaced at upload time with the store's own content blocks (EcomStrait
  builder). Empty here so the section renders nothing by default.
{% endcomment %}

{% schema %}
{
  "name": "Custom content",
  "settings": [],
  "presets": [{ "name": "Custom content" }]
}
{% endschema %}
